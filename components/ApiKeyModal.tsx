// This component is intentionally left blank to resolve placeholder file errors
// while complying with the requirement that the API key must be sourced
// exclusively from environment variables and no UI should be presented to the
// user for entering an API key.
import React from 'react';

const ApiKeyModal: React.FC = () => {
  return null;
};

export default ApiKeyModal;
